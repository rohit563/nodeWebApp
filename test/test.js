var assert = require('assert');
const sinon = require('sinon');
const chance = require("chance");


describe('Validation', function() {

    it('Should create user session for a valid user', function() {
      assert.equal([1,2,3].indexOf(4), -1);
    });
});
